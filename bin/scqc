#!/usr/bin/env python3
"""Entry point for a clone that has not been pip-installed."""
import sys
from pathlib import Path

sys.path.insert(0, str(Path(__file__).resolve().parents[1]))
from scqc_cli import main  # noqa: E402

if __name__ == "__main__":
    sys.exit(main())
